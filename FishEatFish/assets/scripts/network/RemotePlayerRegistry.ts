import type { RemotePlayerState } from './NetworkProtocol.ts';
export interface RemotePlayerView { setPosition(x: number, y: number): void; setRotation(angle: number): void; setHealth?(health: number, maxHealth: number): void; playSkill?(skillId: string): void; playHurt?(): void; playDeath?(): void; playRespawn?(): void; destroy(): void; }
export class RemotePlayerRegistry { private readonly views = new Map<string, RemotePlayerView>(); constructor(private readonly create: (state: RemotePlayerState) => RemotePlayerView) {} upsert(state: RemotePlayerState) { const view = this.views.get(state.playerId) ?? this.create(state); this.views.set(state.playerId, view); view.setPosition(state.x, state.y); view.setRotation(state.rotation); view.setHealth?.(state.health, state.maxHealth); if (state.dead) view.playDeath?.(); } setHealth(playerId: string, health: number, maxHealth: number) { this.views.get(playerId)?.setHealth?.(health, maxHealth); } playSkill(playerId: string, skillId: string) { this.views.get(playerId)?.playSkill?.(skillId); } playHurt(playerId: string) { this.views.get(playerId)?.playHurt?.(); } playDeath(playerId: string) { this.views.get(playerId)?.playDeath?.(); } playRespawn(playerId: string) { this.views.get(playerId)?.playRespawn?.(); } remove(playerId: string) { this.views.get(playerId)?.destroy(); this.views.delete(playerId); } ids(): string[] { return [...this.views.keys()]; } clear() { for (const view of this.views.values()) view.destroy(); this.views.clear(); } }
