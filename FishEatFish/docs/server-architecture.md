# 服务端架构

服务器采用 Node.js 20、TypeScript、Fastify 和 WebSocket。HTTP 负责健康检查、内存账号和匹配，WebSocket 负责房间加入、输入、状态快照和断线移除。

服务器以 20Hz 计算移动，以 10Hz 广播快照。客户端可以预测本地移动，服务器校正位置、旋转、地图边界和输入 tick。

当前默认地图 `sea-default-001` 使用单房间最多 50 人。当前阶段只实现多人可见和同步移动，不实现撕咬、伤害、死亡和复活。
